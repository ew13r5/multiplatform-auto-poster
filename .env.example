# Database
POSTGRES_USER=appuser
POSTGRES_PASSWORD=changeme
POSTGRES_DB=autoposter
DATABASE_URL=postgresql+asyncpg://appuser:changeme@postgres:5432/autoposter
DATABASE_URL_SYNC=postgresql+psycopg2://appuser:changeme@postgres:5432/autoposter

# Redis
REDIS_URL=redis://redis:6379/0

# MinIO
MINIO_ENDPOINT=minio:9000
MINIO_EXTERNAL_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=post-images
MINIO_USE_SSL=false
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin

# Security
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
FERNET_KEYS=your-fernet-key-here
API_KEY=your-api-key-here

# Facebook App (used by split 02)
APP_ID=
APP_SECRET=
GRAPH_API_VERSION=v25.0

# CORS
CORS_ORIGINS=http://localhost:3000

# Alerts (used by split 05)
TELEGRAM_BOT_TOKEN=
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=
